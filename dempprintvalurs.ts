//PRINT VALUES
for (let i = 1; i <= 5; i++) {

    //special inbult function setTimeOut
    //In oder to dealy a task by milliseconds
    setTimeout(function(){
        console.log(i)
    }),6000
    console.log(i)

    let firstNumber=100;
    let secondNumber=100;

    //Equality
    //it checks only the values of the operands
    console.log(firstNumber==secondNumber)
}